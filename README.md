# 📝 Todo List App (React + Vite)

A simple and responsive **Todo List application** built using **React**, **Vite**, and **Bootstrap**.  
This app allows users to add, prevent duplicate todos, and delete todos easily.

---

## 🚀 Features

- ➕ Add new todo items
- 🚫 Prevent empty and duplicate todos
- ❌ Delete todos
- 🎨 Clean UI using Bootstrap
- ⚡ Fast build with Vite
- 🧠 Uses React Hooks (`useState`)

---

## 🛠️ Tech Stack

- **React** (Functional Components)
- **Vite**
- **Bootstrap**
- **JavaScript (ES6+)**
- **CSS**

---

## 📂 Project Structure

" src/
├── Todo-List/
│ ├── todolist.jsx
│ ├── todolist.css
│ └── background.jpg
├── App.jsx
├── main.jsx
public/
├── index.html
└── vite.svg  "


---

## 🧩 How It Works

- `useState` manages the state of the current input and todo list
- `trim()` prevents empty todos
- `includes()` prevents duplicate todos
- `filter()` removes a todo by index
- Adding, deleting, and rendering todos is handled dynamically

---

## ▶️ Getting Started (Run Locally)

1. Clone the repository:
   ```bash
   git clone https://github.com/Soumya-Ranjan-Samaya/my-first-repo.git
